# Appendix reduction plan (generated audit, 2026-09-10). Line numbers refer to paper/iclr/main.tex as of this audit (2514 lines); apply bottom-up.

# Appendix reduction plan (ICLR 2027, main.tex; appendix currently pp. 12-40 = 29 pages)

Conventions: line numbers refer to the current main.tex; apply cuts bottom-up (highest line first) so earlier numbers stay valid. pages_saved = verifiers' revised estimate (conservative choice where they differed). "Realized only with L1" means the saving lands on the deferred-float pages 37-40 and turns into a page reduction only after the layout fix L1 below.

## Tier 1 — safe, do first (both verifiers accept, low risk, no edits outside the cut)

| # | Block | Lines | Action | Instruction | Pages |
|---|---|---|---|---|---|
| C1 | A-B-C | 515-581 | remove | Delete Figure 2 (fig:naive_example) environment; label has 0 refs. | 0.20 |
| C2 | A-B-C | 665-717 | remove | Delete Figure 4 (fig:parity_example) environment; Definition 3.4 already states the rule. | 0.25 |
| A3 | A-B-C | 494-500 | compress | Inline `X=\texttt{BXDQAVZHELNPTKDWSRAJ}` (drop the \[ \] display); delete the last two sentences of line 500 (rule-recovery / audit-purpose restatements); keep the D/V lag-3 walk-through and the "lambda is a discrete abstraction" sentence. | 0.10 |
| E0-3 | E-thm | 858-878 | compress | Keep one display `AUC = AD + 1/2(AC+BD) = 1/2 + 1/2(A-C)`; replace 868-878 with "For rho=1/2, A=1-pi and C=pi, giving AUC*=1-pi." | 0.35 |
| E0-4 | E-thm | 880-906 | remove | Delete the "Alternatively" closed-form derivation, including the stray `\qed` at 906 (prints a mid-proof tombstone). | 0.40 |
| E0-5 | E-thm | 910-921 | compress | Replace the TP/FP/FN/TN align with one sentence: `TP=P(Y*=1,Y=1)=(1-pi)rho, FP=P(Y*=1,Y=0)=pi rho, FN=P(Y*=0,Y=1)=pi(1-rho), TN=(1-pi)(1-rho)`; keep line 908. | 0.20 |
| E0-6 | E-thm | 923-947 | compress | One sentence: "Hence Precision = TP/(TP+FP) = 1-pi, independent of rho, and Recall = TP/(TP+FN) = (1-pi)rho/(rho(1-2pi)+pi), which equals 1-pi at rho=1/2." Delete Observation/Verification blocks. | 0.30 |
| E0-7 | E-thm | 949-981 | compress | One display `F1 = 2TP/(2TP+FP+FN) = 2(1-pi)rho/(pi+2rho(1-pi))` + "which reduces to 1-pi at rho=1/2." Keep 982-983 (`\end{proof}`). | 0.45 |
| E1-3 | E1 | 1065-1088 | compress | Replace five displays with: "Since C'⊆C and E⊆C\C', P(Y*=1|X_1=s_min) − P(Y*=1|X_1=a_0) = P(C)−P(C') = P(C\C') ≥ P(E) > 0." | 0.15 |
| E1-7 | E1 | 1201-1230 | compress | Two sentences: by \eqref{eq:label_noise}, P(Y=1|X_1=a) = pi+(1-2pi)P(Y*=1|X_1=a); pi<1/2 makes the map strictly increasing, so a→P(Y=1|X_1=a) is non-constant and, as before, I(Y;X_1)>0. | 0.20 |
| E23-1 | E2-E3 | 1233-1443 | remove | Delete subsections E.2 and E.3 wholesale (Corollary E.2, its proof, Remarks E.3/E.4). All seven labels have 0 external refs; Discussion line 447 already says no magnitude bound is given; main text line 229 carries the balancing caveat. If you want to keep a trace of the bound, use E23-2 (Tier 3) instead. | 1.80 |
| E4-C5 | E4 | 1616-1638 | compress | Keep the \paragraph heading; replace five displays with two inline sentences (s→∞, |1-2p|<1 ⇒ q_s→1/2 ⇒ P(T=1|X_J)→1/2 uniformly over finite A^{|J|} ⇒ joint law → product ⇒ I(Y*;X_J)=I(T;X_J)→0). | 0.20 |
| F3 | F | 1877-1894 + pointer at 1714 | remove | Delete Table 10 (tab:dl_training) and the sentence "Table~\ref{tab:dl_training} summarizes..." at 1714; all six values are in prose at 1708/1714. | 0.20 |
| F6 | F | 1933-1964 + 1725, 1732 | remove | Delete Table 13 (tab:xgboost_config). At 1725 delete "Table~\ref{tab:xgboost_config} presents..." and add the two table-only items: "using the \texttt{hist} tree method" and "\texttt{scale\_pos\_weight} = N_negative/N_positive". In the Table 6 caption (1732) delete the clause "For XGBoost, we report hyperparameter configuration in Table~\ref{tab:xgboost_config}." | 0.40 |
| H-1 | H | 2226 | remove | Delete the orphan sentence "BERT and LSTM stay within 0.05 of the ceiling across the sweep." (or fold "(within 0.05)" into H-2's merged paragraph). | 0.03 |
| I-1 | I | 2239 | remove | Delete the section-intro sentence; keep `\section` (2237) and `\label{appendix:mechanism_id}` (2238) adjacent. | 0.07 |
| I-4 | I | 2251-2253 | compress | Delete the opening purpose clause "To check whether ... per-rule linear weighting," (start with "We generate 6 synthetic datasets..."); keep protocol, all four AUCs, and the closing conclusion sentence. | 0.02 |
| I10-table | I | 2423-2445 + 2421 | remove | Delete Table 24; trim the per-seed paragraph (2421) to carry every number (Transformer seeds 0/2 .999 both stages, seed 1 .502→.497; LSTM 1.000 all; BERT .504-.508 at n=10, .493-.502 at n=20) plus the two interpretive sentences; retitle I.10 "Length-based curriculum protocol". Realized only with L1. | 0.30 |

Tier 1 total: ≈ 5.6 pages.

## Tier 2 — recommended, minor edits needed (accepted-with-change; exact edit stated)

| # | Block | Lines | Action | Instruction and required edit | Pages |
|---|---|---|---|---|---|
| C3 | A-B-C | 584-663 | remove | Delete Figure 3 (fig:tricky_example). Edit: roadmap row at 471 (see C4). Keep the Appendix A text walk-through (it is what line 169 promises). | 0.30 |
| C4 | A-B-C | 514, 583, 471, 459 | remove | After C1-C3: delete `\section{Compliance Mechanisms}` (514), `\label{appendix:Figures}` (583), the roadmap row `\ref{appendix:Figures} & Ordering Mechanism Example` (471). Edit line 459 (see Global edit G-459). | 0.05 |
| A1 | A-B-C | 490 | compress | Delete the first three sentences (through "...unordered AB cohort."). New lead: "Following \citet{edelman2026order_specific_ehr}, who study order-specific trajectories A→B→C with C a downstream adverse outcome (comparator cohorts as in Section~\ref{sec:related_work}), one validated example is diabetes mellitus → visual disturbance ...". Keep the two disclaimer sentences verbatim. Do not write a literal "Section 2". | 0.08 |
| A2 | A-B-C | 492 | compress | Delete "Our synthetic tasks abstract only..." and "In real EHRs, a model's success may come from..."; in the kept sentence delete the word "therefore" ("We replace event types by letters..."). | 0.05 |
| B1 | A-B-C | 501-513, 470 | merge | Move Appendix B verbatim into Appendix G as `\paragraph{Naive sanity check.}\label{app:naive}` placed after `\end{table}` of tab:results_full (≈2063) and strictly before `\subsection{Real-world audit}` (2078). Delete roadmap row 470. Edit 459 (G-459). | 0.05 |
| D1 | D | 722-732 | compress | Two sentences; define the symbol: "with \hat p(g)=\hat{\mathbb P}(Y=1\mid g) estimated on the training set as in Section~\ref{subsec:models}, ... s_k(X)=(n-k+1)^{-1}\sum_t \hat p(...); unseen k-grams receive the training-set positive rate." Delete the AUC-justification sentence (730b-732). No literal "Section 4.2". | 0.10 |
| D2 | D | 734-741 | compress | Replace with: "Table~\ref{tab:ngram_appendix}: contiguous k-grams peak at k=2 on the compliance tasks (AUC .819 OC-Deterministic, .586 OC-Noisy), stay at chance on GPC-Deterministic for every k (.497–.502), and coverage collapses for k≥5 (41.7%, 1.9%, 0.1%), so longer contiguous patterns cannot be exploited." The Table ref must stay (only ref to Table 4). | 0.07 |
| D3 | D | 768-794 | merge | Delete Table 5 (tab:signal_decomposition) and its paragraph. Append to I.1 (after 2243): "On OC-Deterministic/OC-Noisy, bag-of-events key-letter counts reach AUC .774/.574, XGBoost on letter counts .816/.584, the best contiguous k-gram .819/.586 (Appendix~\ref{appendix:k_gram_analysis}), and L_2 logistic regression on g_{ab}^{(\lambda=7)} .997/.671, matching the best neural model (.999/.671) and the oracle ceiling (1.000/.670) of Table~\ref{tab:results_full}; key-letter counts, letter counts and contiguous k-grams all stay at ≈.50 on GPC-Deterministic (the pair feature was not evaluated there)." Do not add "(L2 LR)"/"26-dim" attributions to the count rows. | 0.35 |
| D4b | D | 761-762, 748-749 | compress | Drop rows k=6,7; replace the caption's last sentence with "Coverage is common across tasks (shared i.i.d. proposal); k=6 and 7 (coverage 1.9%, 0.1%) are omitted, all their AUCs within 0.03 of chance." (Alternative: D4a, Tier 3.) | 0.03 |
| E0-1 | E-thm | 801-812 | layout | Inline the "where A=..., C=..." display using slash notation; add the proof's closed forms Precision*=1−pi and Recall*=(1−pi)rho/(rho(1−2pi)+pi) to the "Similarly, F1*=..." sentence (moved from 926/934). Keep the Theorem environment and label. | 0.03 |
| E0-2 | E-thm | 816-856 (+882) | compress | Replace with ~4 lines as proposed, retaining "where s_+ (resp. s_-) is the score of a random instance with Y=1 (resp. Y=0)" and "B=1−A, D=1−C"; define X:=P(Y=1), Y:=P(Y=0) here and delete the duplicate "Define X=..., Y=..." at 882 (or reduce to "with X, Y as above"). | 0.45 |
| E1-1 | E1 | 988-1022 | compress | Inline the coupling; keep `\paragraph{Case 1}` (996) and "Choose a_0∈A\S" (997); add "by independence of the coordinates" after the =_d identities. Do not touch 1023-1032. | 0.15 |
| E1-2 | E1 | 1023-1063 | compress | Keep C'⊆C argument (inline), the display of E and the P(E)>0 sentence verbatim. Replace 1047-1063 with: "On E, (1,1+λ) is a maximal λ-run of X~ (every other position in {2,…,n} is non-key on E) and it is ordered because κ(s_min)=1 is the minimum rank, so X~ is compliant. In X~', position 1 is a_0∉S and 1+λ is the only key position, so no λ-run has length ≥2 and X~' is not compliant. Hence E⊆C\C'." | 0.14 |
| E1-4 | E1 | 1090-1133 (not 1137) | compress | Inline L, R_1, {Y*=1}=B∪O_1, P(B)<1, κ(s_max)=|A|, p_a; keep one display P(Y*=1|X_1=a)=P(B)+(1−P(B))P(O_1|X_1=a) with the justification that B depends only on coordinates outside R_1 and O_1 only on R_1 (which contains position 1). Keep line 1133; do not touch 1135-1137. | 0.20 |
| E1-5 | E1 | 1135-1188 | compress | Collapse p_{s_min} aligned block (1137-1151) to one line keeping "convention: whole space when L=2"; keep p_{s_max} display; containment as one sentence keeping "the latter event is contained in the former" and "by full support and independence"; keep `(1−P(B))(p_{s_min}−p_{s_max})>0` as one display; delete the "Thus ... ≠ ..." display (1183-1188). | 0.14 |
| E1-6 | E1 | 1187-1199 | compress | One sentence: "In both cases a↦P(Y*=1|X_1=a) is non-constant on A, so Y* and X_1 are not independent; since the mutual information of discrete variables vanishes iff they are independent, I(Y*;X_1)>0." | 0.06 |
| E4-C1 | E4 | 1447-1500 | layout | Prose + one display `Y* = 1 ⊕ ⊕_{k∈K}1{N_k even} = 1 ⊕ (|K| mod 2) ⊕ T`, keeping "where ⊕ denotes XOR"; state the bijection and I(Y*;X_J)=I(T;X_J). | 0.45 |
| E4-C2 | E4 | 1502-1544 | layout | Inline all but the q_s display; keep "s := n−|J| ≥ 1" and "V_J independent of X_J (the B_j are independent)". If r:=|J| is dropped, also rewrite 1620-1622 ("|J| is fixed and s=n−|J|→∞") and 1630 (A^{|J|}). | 0.30 |
| E4-C3 | E4 | 1546-1566 | compress | One sentence ending "...I(Y*;X_J)=I(T;X_J)=0 for every J with 1≤|J|<n." Do NOT put the k-local-purity conclusion here (see E4-C7). | 0.20 |
| E4-C4 | E4 | 1568-1614 | compress | Keep the heading; open with "Since K is nonempty and proper, p∈(0,1); if moreover p≠1/2 then 0<|1−2p|<1, so ..."; keep the two-witness construction inline, no displays. | 0.30 |
| E4-C6 | E4 | 1640-1683 | merge | Keep ONE display `P(Y=1|X_J)=π+(1−2π)P(Y*=1|X_J)` "by \eqref{eq:label_noise}" (self-contained, cf. Appendix~\ref{appendix:proof_impossibility}); state monotonicity, I(Y;X_J)=0 at p=1/2, I>0 at p≠1/2, and the DPI decay inline. Do not touch 1685-1692 here. | 0.30 |
| E4-C7 | E4 | 1685-1692 | remove | Delete the closing paragraph; after "Hence I(Y;X_J)=0." in the p=1/2 observed-label case (≈1644-1648) add: "Since J with 1≤|J|<n was arbitrary, the task is k-locally pure for every k<n (Definition~\ref{def:local_purity})." | 0.08 |
| F1 | F | 1770-1818, 1839-1872, 1711 | merge | One table "Fine-tuning configuration for pretrained models", columns Hyperparameter / LLMs (Llama, Qwen) / BERT; drop Classification-head and Loss blocks (prose at 1768); Memory rows "—" for BERT. Edits: line 1795 "see Loss row" → "see Appendix~\ref{subsec:encoding}"; line 1711 → one sentence "Table~\ref{tab:llm_config} summarizes the fine-tuning configuration for pretrained LLMs and BERT."; delete label tab:bert_config and the stale TODO comment 2412-2415. Alternative: F2 (Tier 3). | 0.45 |
| F4 | F | 1898-1930, 1714 | merge | Merge Tables 11/12 into one 6-column table with two `\multicolumn{6}{l}{\textit{Default/Optimized configuration}}` group rows (not a 7th column); drop the duplicated footnote AND the `$^{*}$` on RNNTransformer; delete label tab:dl_optimal_config; line 1714 → "Table~\ref{tab:dl_default_config} presents the default and optimized architecture configurations." | 0.10 |
| F5 | F | 1822-1834, 1711 | move_to_prose | Delete Table 8; replace the pointer sentence at 1711 in place (pretrained paragraph, not the random-init paragraph) with "The randomly initialized Llama-1M uses hidden size 128, 4 layers, 4 attention heads (2 KV heads), and intermediate size 512." | 0.10 |
| F7 | F | 1702, 1708, 1711/1714 pointers, 1716-1721, 1766 | compress | Delete 1702. Cut 1708 to one sentence (AdamW+weight decay, CE loss; linear schedule with 6% warmup for pretrained) and move "early stopping on validation loss (patience 3)" into 1711; leave 1714's "validation F1, patience 5" untouched. Fold bullets 1716-1721 into one sentence in the random-init paragraph (keep heads, GELU, masked mean pooling, LSTM final state, RNNTransformer projection). At 1766 drop "(q/k/v/o\_proj, r=8)" and the XGBoost-LLM sentence (1725 keeps it with dim 2048). Delete a table-pointer sentence only for tables actually removed/merged. Do NOT touch 1762-1765, 1767, 1768. | 0.15 |
| F8 | F | 1966-1981 | merge | Delete `\subsection` headings/labels appendix:fractions, appendix:hardware; keep the content as `\paragraph{Data fractions and reproducibility.}` and `\paragraph{Hardware.}` inserted after 1728 (before tab:models at 1730); keep the training-time sentence; keep `\FloatBarrier` at 1982. | 0.10 |
| G1 | G | 1999-2013, 510, 1991 | move_to_prose | Delete the Naive block of Table 14. At 510 replace "(Table~\ref{tab:results_full})" with the numbers: at 100% data the Oracle, the 2-gram and all nine neural models evaluated (list them) reach .999–1.000 on all four metrics; XGBoost-basic AUC .603 (.004), F1 .714 (.005), Prec .556 (.004), Rec .995 (.003); XGBoost-LLM AUC .502 (.004), F1 .536 (.008), Prec .501 (.004), Rec .577 (.010). Caption 1991 → "three core datasets". Author: drop or decouple the unsupported "≥40K training examples" clause at 509. Realized only with L1. | 0.20 |
| G2 | G | 2044-2058 | merge | In the GPC block keep 1-gram, XGBoost (basic), XGBoost (LLM), Llama-3.2-1B, Llama-3.1-8B, Llama-3.1-70B* as separate rows; merge BERT, RoBERTa, Llama-1M, Qwen3-4B-Think, LSTM, Transformer, RNNTransformer into one named row (AUC .500–.503, std ≤ .003, F1 .668, Prec .501, Rec 1.000). Realized only with L1. | 0.09 |
| G3 | G | 2000, 2015, 2030, 2045, 1991 | compress | Delete the four Oracle rows; add to caption: "Oracle reference (Theorem~\ref{thm:optimal_performance}): all four metrics equal 1.000 for the three π=0 datasets; for OC-Noisy AUC*=.670, F1*=.577, Prec*=.700, Rec*=.491." Never write a literal "Theorem 1" (it is E.1). | 0.00 |
| G4 | G | 2069-2070 | compress | Caption → panels, "Dashed lines: oracle AUC* (Theorem~\ref{thm:optimal_performance})", shading legend, plus "F1/precision/recall: Table~\ref{tab:results_full}." Keep `\figureorplaceholder{...}{}` with an empty second argument (or `\includegraphics`); do not delete the brace group. Author: the PNG legend lists "Llama-0.2M (cold)", absent from Table 14; the PNG has no chance line. | 0.11 |
| G5 | G | 2085-2116, 2083 | compress | Delete Table 15; replace "See Table~\ref{tab:mimic_ckd_cohort} for an overview." with one sentence carrying 14,813 / 1,146 (7.7%) / 293 CCS; median/mean/max length 49/82/2,396 (54 vs 49 for Y=1/0); median 3 (mean 4.9) admissions, 76% ≥2, 58% ≥3; 265/293 categories shared (Jaccard 0.90), 0 exclusive to progressors, cosine 0.956, Spearman 0.968. Drop 13,667. Realized only with L1. | 0.20 |
| G6 | G | 2121-2147 | compress | Drop the X "Interpretation" column; plain 5-column tabular (`\tabcolsep` 4pt); keep every AUC/F1/Δ value and the split sentence in the caption; keep the LSTM row as one line and move "did not converge reliably on long sequences" into the caption. Realized only with L1. | 0.20 |
| G7 | G | 2149-2171 | compress | Delete the first two sentences of 2150 (restated token/k-gram definitions) but keep "These are descriptive content checks, not validated clinical trajectories." Delete Table 17; fold its four rows into one sentence (CCS_156 30.3% vs 9.2%, log2 +1.71; CCS_87 18.1% vs 7.4%, +1.29; CCS_215 5.5% vs 2.0%, +1.47; cohort-defining CCS_158 99.7% vs 89.9%, +0.15; patient-level prevalence, log2 ratio). Cut 2171 to its last sentence. Do NOT take the full-deletion option. | 0.25 |
| G8 | G | 1987, 2080, 2119, 2174, 2177/2083 | compress | 1987 → one sentence. Delete 2080. 2119 first sentence → "First, contiguous k-gram classifiers as in Section~\ref{subsec:models}, scored per patient by averaging over observed k-grams." Replace `\paragraph{Interpretation.}` + `\paragraph{Limitations...}` by one `\paragraph{Interpretation and limitations.}` starting "The audit results should not be interpreted as evidence that CKD progression is clinically non-temporal, nor as evidence that sequence models are inadequate." then the ICU/diagnosis-codes/access/code sentences. Keep the billing-order caveat once (in Limitations); trim the copy at 2083 instead. | 0.20 |
| H-2 | H | 2182-2184, 2226, 2228-2231 | compress | One paragraph keeping the grid, four models, 400K, three seeds, "using the same hyperparameters as Appendix~\ref{subsec:training_appendix}", "Table~\ref{tab:sensitivity} reports AUC/AUC* (Theorem~\ref{thm:optimal_performance})", the three findings as one clause each (fold in "within 0.05"), and the full medium-difficulty closing sentence. | 0.07 |
| H-3 | H | 2186-2224 | layout | Re-lay Table 18 as 13 columns: shared λ column + three `\multicolumn{4}{c}{m=3/6/10}` groups (AUC*, LSTM, Tr., Llama), `\tabcolsep` 4pt, no `\fitwidth` shrink; caption: "BERT column omitted: its ratio is 1.00 in all 18 configurations. Bold: baseline configuration (m=6, λ=7)"; bold only the four m=6 cells of the λ=7 row. Realized only with L1. | 0.18 |
| I-2 | I | 2241-2247 | merge | Merge I.1/I.2 into `\subsection{Feature ladders}` carrying `\label{appendix:ladder}` (keep appendix:lam_agnostic or drop; 0 refs). Keep one I.1 sentence (classifiers named as they appear, 200,000/50,000, "on the released OC-Deterministic and OC-Noisy splits", the seven families). In I.2 replace the construction sentence by "On the 12\,844-dimensional λ-agnostic feature of baseline (iii), Section~\ref{subsec:models}, ..."; keep 0.997, 303 vs ≤48, 0.632, λ=19, 0.667. Do not write "two classifiers". | 0.08 |
| I5-prose | I | 2261-2264, 2284-2288 | compress | Delete the first sentence of 2261 and the last sentence of 2264; merge Observations + Takeaway into ~120 words keeping: streaming vs fixed identical (so the failure is not a pipeline artifact); at n=10 7/10 seeds reach AUC≈1.0, same seeds {2,4,8} fail in both regimes → initialization-driven bimodality; at n=20 all 20 runs at chance → failing side of a length-driven boundary (\citep{hahn-2020-theoretical}). Point (iii) to "Table~\ref{tab:parity_vocab} (Appendix~\ref{app:parity_vocab})", not "the main-paper result". Heading → "Three observations." if I5-table is applied. | 0.20 |
| I5-table | I | 2266-2282, 2284 | move_to_prose | Delete Table 19; append after the Experimental-grid paragraph: "At n=10 both regimes give AUC .850 (.242) streaming / .850 (.241) fixed with 7/10 seeds succeeding (AUC > 0.9); at n=20 both are at chance (.503 (.004) / .499 (.005), 0/10)." Change the heading at 2284 to "Three observations." (it is the only \ref to tab:parity_reference). Realized only with L1. | 0.18 |
| I678-merge | I | 2290-2359, 478 | merge | One subsection "Parity sweeps: vocabulary, subset size and sequence length" with `\label{app:parity_vocab}` (main text 338); one Setup paragraph with the full I.6 recipe (BERT LoRA 20 ep/batch 32; 2-layer Transformer d=64, 8 heads, ~170K; single-layer LSTM; 30 ep, batch 64, AdamW lr 1e-3; 100K/20K/20K; 3 seeds; keep the one-line data-builder path); one stacked table (Sweep / Setting / BERT / LSTM / Transformer) printing the shared (ℓ=26,|K|=13,n=20) cell in each block with a dagger "same runs"; keep the ℓ=2 row (I.5 refers to it); one Takeaway with one conclusion clause per control (not a vocabulary effect; |K|=2→4 transition with |K|=6 on the failing side; balanced regime at chance at every n including n=10, unlike binary parity in I.5; Theorem 3.5). Mandatory: roadmap line 478 → `\ref{appendix:parity_reference}--\ref{app:parity_vocab}`. Realized partly with L1. | 0.55 |
| I9-prose | I | 2395-2401 | compress | One ~120-word paragraph beginning "Table~\ref{tab:fewshot} shows ..." (only ref to Table 23): OC-Det 20-shot .53–.56 for all three vs .995 (fine-tuned Llama-3.1-8B) / .999 (BERT) → access mode dominates; OC-Noisy ≤ .525 (oracle .670, reached by fine-tuned BERT); GPC at chance in every cell; closing sentence "gap large on the learnable tasks, vanishes on the unlearnable one". Drop "+0.08 lift" (actual mean is +0.07). | 0.20 |
| I9-protocol | I | 2364-2367, 2371 | compress | Fold the intro into the Protocol keeping "To rule out that the patterns of Table 14 are artefacts of the fine-tuning access mode ... without any parameter updates"; drop "F_1, and accuracy" and the ±0.015 CI clause but KEEP "1000 examples per (τ,M,k) cell, drawn once from the paper test split per seed"; caption → first sentence + the "—" note. | 0.08 |
| I10-hparams | I | 2408-2419 | compress | Replace the itemize with: "Transformer and BERT configurations and the training schedule are those of Appendix~\ref{app:parity_vocab} (Transformer d_ff=512, dropout 0.3, 168,642 / 169,282 parameters at n=10 / n=20; lr 1e-3, batch 64, 30 epochs, early-stopping patience 3 on validation loss); the LSTM uses the same training schedule; 3 seeds per architecture per stage." Delete the TODO comment 2412-2415. Do not name the optimizer until the Adam/AdamW discrepancy is reconciled (see inconsistencies). | 0.10 |
| J1 | J | 2454-2486 | compress | One ~160-word paragraph from existing sentences: scope sentence; setup (keep "unmasked target positions", three classes, 10,000 held-out OC-Deterministic sequences, Figure~\ref{fig:attn_layer_summary}); finding 1; finding 2; stratification null; magnitude caveat. Delete 2470-2471, 2476-2477, 2484-2486. Alternative: J3 (Tier 3). | 0.20 |
| J2 | J | 2502-2510 | compress | Caption → legend only: "... Red: lag-connected key targets at distance λ (unmasked positions); blue: other key targets; gray: non-key targets. Panels use separate y-axis ranges and are not directly comparable. Left: BERT-base; right: Llama-3.2-1B." Delete the two subfigure `\caption{}` lines, keep the subfigure wrappers. | 0.06 |

Tier 2 total: ≈ 7.9 pages.

Global edit G-459 (needed by C4, B1, and any appendix removal): line 459 hard-codes "Appendices A--F ... Appendix G ... H ... I ... J". Replace once, after all cuts are decided, with `\ref{}`-based text (e.g. "Appendices~\ref{app:illustrative}--\ref{appendix:training} ...; Appendix~\ref{appendix:results} ...; Appendix~\ref{app:sensitivity} ...; Appendix~\ref{appendix:mechanism_id} ...; Appendix~\ref{appendix:attention} ...") so it cannot desynchronize. grep confirms it is the only hard-coded appendix letter in the file.

## Tier 3 — optional / judgment call (alternatives to Tier 1/2 items; increments are relative to the alternative)

| # | Block | Lines | Action | Trade-off | Pages |
|---|---|---|---|---|---|
| J3 | J | 2451-2513 + 433, 459, 481 | remove | Delete Appendix J and Figure 6; also delete the one sentence at 433 ("Internally, attention analysis confirms ... (Appendix~\ref{appendix:attention})."), the last sentence of 459, and roadmap row 481. Both verifiers accept; the paper loses its only model-internal observation, which the authors already disclaim as non-evidential. Replaces J1+J2 (+0.74 net). Being the last text section, its removal also frees the p.36 blank. | 1.00 |
| F2 | F | 1770-1818, 1839-1872, 1711 | remove | Delete Tables 7 and 9 outright (all values are in prose at 1708/1711/1768; add one sentence: "LoRA bias is disabled (PEFT task type CAUSAL_LM for decoders, adapter-config only; SEQ_CLS for BERT); inputs are padded/truncated to 200 tokens; for decoder LLMs the KV cache is disabled during training."). Medium risk: reviewers often expect a LoRA hyperparameter table. Replaces F1 (+0.45 net). Confirm Table 9's "(when used)" hedge is a leftover before dropping. | 0.90 |
| I678-fold | I | (after I678-merge) | move_to_prose | Keep only the |K|-sweep rows; state the ℓ-sweep and n-sweep as two range sentences ("15 cells × 3 seeds; mean AUC .497–.508, std ≤ .008"; n∈{10,15,30}: ".497–.503"), keep the n=10 balanced-vs-binary contrast sentence, delete the two "Results are reported in Table ..." sentences. Medium risk: per-cell numbers become ranges. Additive to I678-merge. | 0.30 |
| D4a | D | 743-766, 734 | compress | Replace Table 4 by one sentence carrying the full sweep (k=1..7 AUC .813/.819/.818/.781/.617/.524/.501 OC-Det; .585/.586/.576/.534/.508/.499/.501 OC-Noisy; all k at chance .497–.502 on GPC; coverage 100% for k≤4, 41.7%/1.9%/0.1% for k=5/6/7) and remove the Table ref at 734. Reviewer prefers keeping a table; replaces D4b (+0.17 net). | 0.20 |
| E23-2 | E2-E3 | 1268-1443 | compress | If you do NOT take E23-1: keep Corollary E.2's statement (1233-1266), replace the pointer at 1268 with a 3-4 line proof sketch pointing to Case 1 of Appendix E.1 (E⊆C\C', P(E)=(m/ℓ)((ℓ−m)/ℓ)^{n−2} by independence and uniformity with Z_t~Uniform(A); noisy bound from the identity at the end of E.1), delete 1270-1443. Alternative to E23-1 (−0.4 relative). | 1.40 |
| E23-3/4/5 | E2-E3 | 1275-1365; 1367-1394; 1396-1440 | compress/remove | Fallback if E.2 AND E.3 are kept: compress the E.3 proof body to ~10 lines pointing to Case 1 of E.1 (say "Z_t iid Uniform(A)"); delete Remark E.3 (full env 1367-1394, not 1367-1391) keeping its last sentence as plain text after `\end{proof}`; delete Remark E.4 (env is 1396-1440, NOT 1393-1443, which would truncate Remark E.3) or keep the two-sentence AUC-invariance version. Then also apply E1-8's E.3 pointer wording. Sum 0.55+0.15+0.45. | 1.15 |

Tier 3 increment if every alternative is chosen over its Tier 1/2 counterpart (J3, F2, I678-fold, D4a): ≈ +1.7 pages.

## Cumulative totals (line-based estimates; expect float quantization to eat 1-2 pages)

- Tier 1: ≈ 5.6 pages → appendix ≈ 23 pages.
- Tier 1 + 2: ≈ 13.5 pages → appendix ≈ 15.5 pages (realistic target after recompiling: 16-17 pages; document ≈ 27-28 pages).
- Tier 1 + 2 + all Tier 3 alternatives: ≈ 15 pages → appendix ≈ 14 pages.
- Do not count E1-8 (subsumed by E23-1/E23-2), D5 (subsumed by D3), H-1 (folded by H-2) or E1-9 (fallback) separately.

## Layout fixes (no content; apply regardless of tier, they are what turns float-page savings into page-count savings)

- L1 (root cause, mandatory): line 1989 `\begin{table}[tb]` → `\begin{table}[tbp]` for Table 14 (tab:results_full). The log reports "Float too large for page by 5.92pt" for it; without 'p' it can never be placed and, since floats are placed in order, ALL later tables (15-24) are deferred to pp. 37-40. Verified by recompiling: with [tbp] Table 14 lands on p.31 and Tables 15-24 flow beside their text; on its own it does not drop a page (slack consolidates on p.40) but afterwards ~0.2 page of content cuts already yields 39 pages, versus ~0.5 page without it. (G9, I-floats.)
- L2: do NOT add `\FloatBarrier` at the end of Appendix C (line ~718; costs ~0.2 page) or at the end of Section I (line 2445; pins J after the float pages). A `\FloatBarrier` before `\section{Sensitivity analysis}` (2179) is harmless once L1 is in.
- L3 (if D3 is not taken): line 773 `[tb]` → `[htb]` for Table 5 removes ~10 blank lines on p.15 (≈0.13 page). Subsumed by D3.
- L4 (E0-8): in the Theorem E.1 proof use `\[ \]`/`align*` so no unreferenced equation numbers (2)-(40) remain; remove the `\qed` at 906 (done by E0-4).
- L5 (E1-9, only if E1-3/4/5/7 are not adopted): collapse the aligned blocks at 1078-1088, 1111-1117, 1137-1151, 1174-1182 to single-line displays; block 1202-1217 must stay a 4-line align* (a one-liner overflows by 445pt). ≈0.2 page.
- L6 (I-3, optional, ≈0.07): convert I.1-I.4 `\subsection` to `\refstepcounter{subsection}\paragraph{\thesubsection\quad ...}` run-in headings, keeping each `\label` on the next line; this preserves the I.1-I.4 numbering and main-text refs, so roadmap line 477 needs no edit. Plain `\paragraph` would renumber I.5-I.10 and render 477 as "I--I".
- L7 (J5, if J is kept): line 2489 `\begin{figure}[tb]` → `[htbp]` in place; the figure then follows the text instead of splitting it. Moving it after paragraph 1 with [htbp] changes nothing; `[H]` works (float.sty is loaded via algorithm.sty) only if the figure is moved after line 2463.
- F9 (rejected): `\small`/`\footnotesize` on the config tables does not save space (ICLR `\small` is already 9pt; the test compile got longer).

## Rejected cuts (considered, not recommended)

- E1-10 (delete Case 2, S=A, of the Theorem 3.3 proof): theorem is stated for S⊆A (setup line 174; Naive uses m=ℓ), so the proof would be incomplete; also the stated range ends mid-display.
- F9 (\small + [htbp] for config tables): test compile shows zero saving (appendix gets ~0.06 page longer); ICLR `\small` = `\footnotesize` = 9pt.
- G4b (delete Figure 5 entirely): it is the only place the Transformer/RoBERTa/RNNTransformer/Qwen3-4B/Llama-1M learning curves appear; Table 2's data-efficiency star for the Transformer would be unsupported. Use G4.
- H-3b (drop λ∈{3,9} rows of Table 18): selective reporting, makes "full sweep" false, removes the λ=9 recovery cell, and saves no page (Table 18 is on a float page). Use H-3.
- J4 (delete Figure 6 but keep J's text): main-text line 433 says "attention analysis confirms"; the figure is the only displayed evidence. Use J1+J2 or J3.

## Pre-existing inconsistencies surfaced by the verifiers (reconcile from your logs; nothing may be invented)

- 2-gram baseline: Table 4 gives .819/.586, Table 14 .821/.585, main text 0.82/0.585 (line 399 cites .585 but Table 4 k=2 OC-Noisy is .586).
- Main text 433 quotes content-count AUCs 0.82/0.58, a within-group AUC 0.65 vs 0.50 after matching on key-letter counts, and "three simple classifiers"; none of these appear in Appendix I.1-I.4 (I.1 says "two classifiers"). D3 supplies .816/.584 only.
- Main text 437 says the curriculum fails "for pretrained BERT or Llama"; I.10 contains no Llama curriculum run. Drop "or Llama" or supply the run.
- Early-stopping criterion: 1708 says validation loss for all models; 1714 and Table 10 say validation F1 for random-init models.
- BERT batch size: 1711/Table 9 say 16; 2295 and 2416 say 32.
- Optimizer: I.5 (2264) and I.10 (2410) say Adam; I.6 (2295), App. F (1708) and main text 373 say AdamW. The reviewer's repo check found the shared parity script uses `torch.optim.Adam`, a 2-layer LSTM (hidden 128, dropout 0.5, lr 5e-4), not the "single-layer LSTM, lr 1e-3" of I.6; reconcile I.6 against the actual runs before I.10 defers to it.
- Figure 5 legend lists "Llama-0.2M (cold)" (absent from Table 14 and the 13-model list) and draws no chance line on GPC although the caption says "or chance".
- Appendix B line 509 claims near-perfect Naive AUC "with ≥40K training examples"; no Naive learning curve or per-fraction table exists.
- Theorem thm:optimal_performance is Theorem E.1 (theorems numbered per section); never write "Theorem 1" in new caption text.
- Figure 4 (fig:parity_example) source has a stale comment ("U at position 9"); moot after C2.