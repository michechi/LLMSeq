# Append-only result CSVs: keep both row sets on cross-site merges
results/matched_completion/*.csv merge=union

# KIP results are tracked; parallel FOX appends union-merge (ported 2026-09-14)
results/kip_training.csv merge=union
results/kip_training_p5.csv merge=union
results/kip_training_fullft.csv merge=union
